# 4Key Web Rhythm Game — Product Requirements Document

> 문서 버전: 1.0  
> 작성 기준일: 2026-08-21  
> 초기 콘텐츠: ARForest — The Last Page / YUGEN 스킨

## 1. 제품 개요

### 1.1 제품 정의

PC 웹 브라우저에서 키보드의 네 개 키로 플레이하는 4Key 리듬게임이다. 사용자는 음악에 맞춰 내려오는 일반 노트와 롱 노트를 입력하고, 판정·점수·콤보·정확도·HP를 통해 결과를 확인한다.

첫 번째 버전은 **한 곡만 완성도 있게 플레이할 수 있는 게임**을 목표로 한다. 곡 수를 늘리는 대신 현재 제공된 `ARForest - The Last Page`의 네 난이도와 `YUGEN` 스킨을 정확히 지원하는 데 집중한다.

### 1.2 제품 목표

- 별도 설치 없이 최신 PC 브라우저에서 실행한다.
- 음악과 노트가 체감상 어긋나지 않는 안정적인 플레이를 제공한다.
- Easy부터 Insane까지 네 난이도를 선택할 수 있다.
- YUGEN 스킨의 4Key 노트·레인·판정 이미지를 사용한다.
- 플레이 결과와 개인 최고 기록을 제공한다.
- 기반 기능 완성 후 온라인 리더보드와 1대1 실시간 대전으로 확장한다.

### 1.3 초기 버전에서 하지 않는 것

- 두 번째 곡 또는 사용자 곡 업로드
- 4Key 이외 키 모드
- `.osb` 스토리보드 재생
- osu! 클라이언트의 모든 스킨·점수 규칙 완전 호환
- 모바일·터치 입력 지원
- 회원 프로필, 친구, 채팅, 공개 매치메이킹
- 맵 또는 스킨 편집기

## 2. 제공 콘텐츠 분석

### 2.1 곡

| 항목 | 값 |
|---|---|
| 제목 | The Last Page |
| 아티스트 | ARForest |
| 비트맵 제작자 | PokeSky |
| Beatmap Set ID | 1276332 |
| 게임 모드 | Mania (`Mode: 3`) |
| 키 수 | 4 (`CircleSize: 4`) |
| BPM | 195 |
| 음원 | `audio.mp3` |
| 배경 | `bg.jpg` |
| 미리듣기 시작 | 98.934초 |
| 노트 플레이 구간 | 약 129.08초 |

### 2.2 난이도

| 난이도 | Beatmap ID | OD / HP | 전체 노트 | 롱 노트 |
|---|---:|---:|---:|---:|
| Easy | 2654029 | 6.5 / 6.5 | 547 | 81 |
| Normal | 2652890 | 7.0 / 7.0 | 831 | 108 |
| Hard | 2652889 | 7.5 / 7.5 | 1,214 | 25 |
| Insane | 2651800 | 8.0 / 8.0 | 1,408 | 152 |

네 난이도 모두 같은 음원, 배경, BPM과 플레이 구간을 사용한다. 사용자는 곡을 다시 고르는 대신 난이도를 바꾸며 반복 플레이한다.

### 2.3 스킨

YUGEN 스킨의 이름은 `- YUGEN FINAL - Widescreen`, 제작자는 `[Garin]`, 버전은 `2.4`이다. 4Key 설정은 폭 45px의 동일한 네 레인, 하단 판정선, 어두운 레인과 녹색·파란색 키 조명을 정의한다. 일반 노트, 롱 노트의 head/body/tail, 눌림 키, 스테이지, 조명, 판정 이미지가 포함되어 있다.

현재 스킨 폴더는 4Key 게임과 향후 확장에 필요한 자산만 남긴 정리본이다. 최상위 `- YUGEN -/`가 유일한 스킨 루트이며, 내부 중복 복사본과 Standard·Taiko·Catch 전용 이미지는 제거되었다. 남은 파일은 PNG 406개, WAV 48개, JPG 4개, MP3 3개, 설정 파일 1개와 스킨 데이터베이스 1개다.

화면별 주요 자산은 다음과 같다.

| 화면/기능 | 사용할 자산 |
|---|---|
| 시작·메뉴 | `menu-background*`, `menu-button-background*`, `mode-mania*` |
| 난이도 선택 | `selection-*`, `songselect-bottom*`, `star*` |
| 플레이 | `mania-note*`, `mania-key*`, `mania-stage*`, `mania-hit*` |
| HUD | `score-*`, `combo-*`, `scorebar-*`, `inputoverlay-*` |
| 일시정지·실패 | `pause-*`, `fail-background.png` |
| 결과·리더보드 | `ranking-*`, `section-pass*`, `section-fail*` |

### 2.4 스킨 오디오

스킨 오디오는 화면 효과음과 노트 타격음으로 나누어 사용한다.

- 메뉴: `welcome.wav`, `menuclick.wav`, `menuhit.wav`, `menuback.wav`, `whoosh.wav`
- 시작 연출: `ready.mp3`, `count3s.wav`, `count2s.wav`, `count1s.wav`, `gos.wav`
- 플레이: `soft-hitnormal.wav`, `combobreak.wav`
- 결과: `sectionpass.wav`, `sectionfail.wav`; 긴 결과 음악인 `applause.mp3`, `failsound.mp3`는 선택 로딩
- 멀티플레이 확장: `match-confirm.wav`, `match-start.wav`

현재 네 맵은 모두 Soft 샘플셋을 사용한다. Normal·Drum 샘플은 향후 다른 4Key 맵을 지원할 때를 위해 유지하지만 MVP의 필수 로딩 대상은 아니다.

## 3. 사용자와 핵심 경험

### 3.1 대상 사용자

- 키보드 리듬게임을 처음 접하는 사용자
- 한 곡의 여러 난이도에서 기록을 높이고 싶은 사용자
- 설치 없이 짧게 플레이하고 싶은 PC 사용자
- 최종적으로 친구와 같은 맵으로 대결하고 싶은 사용자

### 3.2 핵심 사용자 흐름

1. 웹 페이지에 접속한다.
2. 브라우저의 오디오 재생 허용을 위해 시작 버튼을 누른다.
3. The Last Page의 배경·제목과 네 난이도를 확인한다.
4. 난이도, 키, 스크롤 속도, 오프셋과 음량을 설정한다.
5. 준비 화면 후 카운트다운을 거쳐 플레이한다.
6. 일반 노트는 해당 키를 누르고, 롱 노트는 끝까지 유지한다.
7. 종료 후 점수, 정확도, 최대 콤보와 판정 수를 확인한다.
8. 같은 난이도를 재도전하거나 다른 난이도를 선택한다.

## 4. 기능 요구사항

### FR-01 시작 및 리소스 로딩

- 첫 사용자 입력 이후 AudioContext를 활성화한다.
- 음원, 선택 난이도의 맵, 배경, 필수 스킨 이미지와 짧은 필수 효과음을 로딩한다.
- 진행률과 현재 로딩 대상을 표시한다.
- 필수 리소스 실패 시 파일 종류와 재시도 버튼을 제공한다.
- 결과용 장시간 MP3와 멀티플레이 효과음은 해당 화면 또는 기능 진입 전까지 로딩하지 않는다.

### FR-02 난이도 선택

- 단일 곡의 Easy, Normal, Hard, Insane을 선택할 수 있다.
- 난이도별 OD, HP, 노트 수와 롱 노트 수를 표시한다.
- 98.934초부터 짧은 미리듣기를 재생하고 중지할 수 있다.
- 마지막으로 선택한 난이도를 브라우저에 저장한다.

### FR-03 4Key 플레이

- 기본 키는 `D`, `F`, `J`, `K`이다.
- 일반 노트, 롱 노트, 동시 입력을 지원한다.
- 길게 누른 키의 자동 반복은 추가 입력으로 처리하지 않는다.
- 누름과 뗌을 모두 추적하여 롱 노트를 처리한다.
- 음악 재생 시간에 맞춰 노트가 판정선에 도착한다.

### FR-04 판정

- 입력 시점과 노트 시점의 차이로 `Perfect`, `Great`, `Good`, `Miss`를 결정한다.
- 지나간 미입력 노트는 자동으로 Miss 처리한다.
- 롱 노트는 시작 입력과 유지 성공 여부를 반영한다.
- 판정 구간은 모든 난이도에서 일관된 앱 규칙을 사용하며 TRD의 상수로 관리한다.
- 사용자가 설정한 오프셋을 판정과 렌더링에 동일하게 적용한다.

### FR-05 점수·콤보·정확도·HP

- 판정마다 점수와 정확도를 갱신한다.
- Miss가 아닌 연속 입력은 콤보를 늘리고 Miss는 콤보를 초기화한다.
- 현재 콤보와 최대 콤보를 구분한다.
- 좋은 판정은 HP를 회복하고 Miss는 HP를 감소시킨다.
- HP가 0이 되어도 초기 버전에서는 곡을 끝까지 플레이할 수 있으며 결과에 `Failed`를 표시한다.
- 점수 공식은 난이도 사이의 리더보드 비교가 가능하도록 최대 기본 점수를 1,000,000으로 정규화한다.

### FR-06 플레이 화면

- YUGEN 4Key 스킨을 적용한 레인, 키, 노트, 롱 노트와 판정 효과를 표시한다.
- 배경 이미지는 비율을 유지해 화면을 채우고, 플레이 영역 가독성을 위한 어두운 오버레이를 적용한다.
- 점수, 콤보, 정확도, HP, 곡 진행률을 표시한다.
- `Escape`로 일시정지하며 계속하기, 재시작, 난이도 선택으로 이동할 수 있다.
- 포커스를 잃으면 자동으로 일시정지한다.
- 메뉴 선택, 시작 카운트다운, 노트 입력, 콤보 단절과 결과 상태에 대응하는 YUGEN 효과음을 재생한다.
- 효과음이 짧은 시간에 중첩되더라도 음악 재생 시계와 판정에는 영향을 주지 않는다.

### FR-07 결과 및 로컬 기록

- 결과 화면에 점수, 정확도, 최대 콤보, Perfect/Great/Good/Miss 수, 클리어 상태를 표시한다.
- 난이도별 개인 최고 점수와 최고 정확도를 LocalStorage에 저장한다.
- 최고 기록을 갱신하면 이를 명확하게 표시한다.
- 재도전과 난이도 선택 버튼을 제공한다.

### FR-08 설정

- 네 레인의 키를 각각 변경할 수 있으며 중복 키는 허용하지 않는다.
- 스크롤 속도, 입력 오프셋, 마스터 음량과 효과음 음량을 변경할 수 있다.
- 음악과 효과음을 각각 음소거할 수 있다.
- 기본값 복원 기능을 제공한다.
- 설정은 LocalStorage에 저장하고 다음 방문에 복원한다.

### FR-09 온라인 리더보드 — 2단계

- 난이도별 상위 기록을 표시한다.
- 익명 사용자에게 중복되지 않는 ID와 변경 가능한 표시 이름을 부여한다.
- 한 사용자의 같은 난이도 기록은 최고 점수만 순위에 반영한다.
- 점수가 같으면 정확도, 최대 콤보, 먼저 달성한 시각 순으로 정렬한다.
- 비정상 값과 과도한 제출을 거부한다.

### FR-10 1대1 실시간 대전 — 최종 단계

- 한 사용자가 방 코드를 만들고 다른 사용자가 참가한다.
- 두 사용자 모두 준비하면 동일 난이도와 서버 기준 시작 시각으로 시작한다.
- 플레이는 각 브라우저에서 로컬로 진행하고 상대의 점수·콤보·HP 진행 정보만 실시간 표시한다.
- 양쪽 종료 후 점수, 정확도와 판정을 비교하고 승자 또는 무승부를 표시한다.
- 상대가 이탈하면 재접속 대기 후 기권 결과를 표시한다.

## 5. 비기능 요구사항

### 성능

- 1920×1080 기준 일반 플레이 중 평균 60FPS를 목표로 한다.
- Insane의 1,408개 노트에서도 화면 밖 노트를 그리지 않는다.
- 게임 시간은 프레임 수가 아니라 오디오 시계를 기준으로 한다.
- 같은 세션에서 재도전할 때 음원과 스킨 이미지를 다시 디코딩하지 않는다.

### 호환성과 접근성

- 최신 Chrome과 Edge를 필수 지원하고 Firefox를 검증 대상에 포함한다.
- 최소 화면 폭은 1024px이며 키보드가 연결된 PC 환경을 대상으로 한다.
- 메뉴는 마우스와 키보드로 조작할 수 있어야 한다.
- 색상만으로 레인을 구분하지 않고 위치와 키 라벨을 함께 사용한다.

### 안정성과 개인정보

- 리소스 오류가 전체 페이지의 무반응으로 이어지지 않아야 한다.
- 온라인 기능 이전에는 서버나 개인정보 수집이 없다.
- 온라인 기능에서는 익명 ID, 표시 이름과 플레이 결과만 저장한다.

## 6. 개발 단계 및 완료 기준

### 1단계 — 한 곡 싱글 플레이 MVP

- 네 난이도가 모두 끝까지 재생된다.
- 일반·롱·동시 노트, 판정, 점수, 콤보, 정확도와 HP가 동작한다.
- YUGEN 스킨과 배경이 적용된다.
- 설정과 난이도별 최고 기록이 저장된다.
- 일시정지, 재시작과 결과 화면이 동작한다.

### 2단계 — 온라인 리더보드

- 익명 사용자 식별과 표시 이름 설정이 가능하다.
- 난이도별 개인 최고 기록 제출과 상위 순위 조회가 가능하다.
- 데이터 접근 정책과 기본적인 부정 제출 검사가 적용된다.

### 3단계 — 1대1 실시간 대전

- 방 생성부터 참가, 준비, 동시 시작, 진행 표시와 결과 확정까지 한 흐름으로 동작한다.
- 두 브라우저에서 시작 시각 차이가 허용 범위 안에 있다.
- 이탈, 만료와 재접속 실패를 사용자에게 설명한다.

## 7. 성공 지표

- 네 난이도 완주 성공률과 치명적 오류 없는 세션 비율
- 오디오와 노트의 체감 동기화 관련 오류 수
- 난이도별 재도전 비율
- 플레이 중 평균 FPS와 긴 프레임 발생 횟수
- 온라인 단계 이후 리더보드 제출 성공률과 대전 정상 종료율

## 8. 콘텐츠 및 권리 관련 전제

현재 포함된 음원, 비트맵, 배경과 스킨은 개발용 리소스로 취급한다. 공개 배포 전에 각 리소스의 사용·재배포 허가를 프로젝트 담당자가 확인해야 한다. 제품은 osu!의 공식 서비스가 아니며 공식 호환성을 주장하지 않는다.
